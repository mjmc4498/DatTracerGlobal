# DatTracer Global

Aplicación MVC local para trazabilidad SQL y linaje básico de datos.

## Requisitos
- Python 3.10+

## Instalación
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Ejecución
```bash
python app.py
```

Luego visita `http://localhost:5000`.
